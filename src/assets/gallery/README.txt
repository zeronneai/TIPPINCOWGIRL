Drop gallery photos here, named 01–05.
Ideal: a WebP + JPG pair per slot (01.webp + 01.jpg). A single .webp/.jpg/.png also works.
Slot 01 is vertical (2 rows tall), slot 05 is wide (2 columns). Keep photos <= ~1600px wide.
They replace the placeholder tiles automatically on the next build.
